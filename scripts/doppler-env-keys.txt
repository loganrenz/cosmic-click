# Doppler keys (no .env). Template uses project nuxt_template, config base.

# --- nuxt_template (shared): add these once ---
# GSC_SERVICE_ACCOUNT_JSON = base64 from: node scripts/doppler-b64-key.mjs ./path/to/key.json
GA_ACCOUNT_ID
POSTHOG_PERSONAL_API_KEY
POSTHOG_HOST
# Single PostHog project for all apps (no per-app projects): set POSTHOG_PUBLIC_KEY in nuxt_template;
# setup-new-app copies it into each app. Each app sets APP_NAME so events are partitioned (filter by "app" in PostHog).
POSTHOG_PUBLIC_KEY
GSC_SERVICE_ACCOUNT_JSON
GSC_USER_EMAIL
# Optional: APPLE_*, NUXT_PUBLIC_MAPKIT_TOKEN

# --- Per-app Doppler project (e.g. simple-game) ---
# setup-new-app sets: SITE_URL, APP_NAME, and copies POSTHOG_PUBLIC_KEY from nuxt_template (single shared project).
# setup:all writes GA_MEASUREMENT_ID into this project.
SITE_URL
APP_NAME
# POSTHOG_PUBLIC_KEY copied from nuxt_template (shared project); events partitioned by APP_NAME.

# --- Full key list (for copy into new project) ---
APP_URL
APPLE_TEAM_ID
APPLE_CLIENT_ID
APPLE_KEY_ID
APPLE_SECRET_KEY
POSTHOG_PUBLIC_KEY
POSTHOG_PERSONAL_API_KEY
POSTHOG_HOST
GA_MEASUREMENT_ID
GA_ACCOUNT_ID
GSC_SERVICE_ACCOUNT_JSON
GSC_USER_EMAIL
SITE_URL
NUXT_PUBLIC_MAPKIT_TOKEN

# --- Next test run: checklist (fix issues so “bingo boom shakalaka”) ---
# [x] nuxt_template must have: GA_ACCOUNT_ID, POSTHOG_PERSONAL_API_KEY, GSC_SERVICE_ACCOUNT_JSON (base64), GSC_USER_EMAIL
# [ ] New app: create Doppler project; config: doppler configs create dev_base --project <name> --environment dev; set SITE_URL; doppler.yaml: project <name>, config dev_base
# [ ] setup:all: SITE_URL=https://<app>.pages.dev doppler run --project nuxt_template --config base -- npm run setup:all
# [ ] Before first deploy: wrangler pages project create <name> --production-branch main
# [ ] Deploy: doppler run --project <app> --config dev_base -- npm run deploy
# [ ] After deploy: SITE_URL=... doppler run --project nuxt_template --config base -- npm run setup:gsc:verify
